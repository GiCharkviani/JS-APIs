/* CommonJS Modules */

// they are used in NodeJS
// here is also one file one module, like ES6 Modules

// export is just a object that is not defined neither here nor in browser
// but in NodeJS it's improtant object to use

/*
export.addToCart = function(product, quantity) {
    cart.push({product, quantity});
    console.log(`${quantity}  ${product} added to cart`)
}
*/

// importing:
// require is also defined in NodeJS
/*
const { addToCart } = require('./smth.js')
*/
